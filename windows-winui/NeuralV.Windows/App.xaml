<Application
    x:Class="NeuralV.Windows.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0E1320" />
        <SolidColorBrush x:Key="AppBackgroundAltBrush" Color="#151C2D" />
        <SolidColorBrush x:Key="AppSurfaceBrush" Color="#171E30" />
        <SolidColorBrush x:Key="AppSurfaceRaisedBrush" Color="#20283C" />
        <SolidColorBrush x:Key="AppSurfaceStrongBrush" Color="#27324A" />
        <SolidColorBrush x:Key="AppSurfaceHighBrush" Color="#32405A" />
        <SolidColorBrush x:Key="AppCardBrush" Color="#2D3852" />
        <SolidColorBrush x:Key="AppChromeBrush" Color="#131A2A" />
        <SolidColorBrush x:Key="AppAccentBrush" Color="#7C6BFF" />
        <SolidColorBrush x:Key="AppAccentSecondaryBrush" Color="#57D5F9" />
        <SolidColorBrush x:Key="AppAccentTertiaryBrush" Color="#F7A879" />
        <SolidColorBrush x:Key="AppPrimaryContainerBrush" Color="#46589B" />
        <SolidColorBrush x:Key="AppSecondaryContainerBrush" Color="#2F6574" />
        <SolidColorBrush x:Key="AppTertiaryContainerBrush" Color="#73543E" />
        <SolidColorBrush x:Key="AppAccentSoftBrush" Color="#33415B" />
        <SolidColorBrush x:Key="AppAccentMutedBrush" Color="#26354C" />
        <SolidColorBrush x:Key="AppOutlineBrush" Color="#44506B" />
        <SolidColorBrush x:Key="AppOutlineStrongBrush" Color="#687698" />
        <SolidColorBrush x:Key="AppTextBrush" Color="#F5F7FC" />
        <SolidColorBrush x:Key="AppMutedTextBrush" Color="#C1C9E1" />
        <SolidColorBrush x:Key="AppSubtleTextBrush" Color="#95A0BC" />
        <SolidColorBrush x:Key="AppOnSurfaceVariantBrush" Color="#D7DDF0" />
        <SolidColorBrush x:Key="AppOnAccentBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="AppSuccessBrush" Color="#54D195" />
        <SolidColorBrush x:Key="AppWarningBrush" Color="#FFB95F" />
        <SolidColorBrush x:Key="AppDangerBrush" Color="#FF829F" />
        <SolidColorBrush x:Key="AppOverlayScrimBrush" Color="#9C0A0E18" />
        <LinearGradientBrush x:Key="AppSurfaceGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#20283C" Offset="0" />
            <GradientStop Color="#171E30" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AppSurfaceStrongGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#32405A" Offset="0" />
            <GradientStop Color="#20283C" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AppAccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#7C6BFF" Offset="0" />
            <GradientStop Color="#57D5F9" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AppAccentSoftGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#46589B" Offset="0" />
            <GradientStop Color="#2F6574" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AppSecondaryGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#27324A" Offset="0" />
            <GradientStop Color="#32405A" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AppFieldGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#20283C" Offset="0" />
            <GradientStop Color="#171E30" Offset="1" />
        </LinearGradientBrush>

        <CornerRadius x:Key="WindowShellCornerRadius">36</CornerRadius>
        <CornerRadius x:Key="SurfaceCardCornerRadius">30</CornerRadius>
        <CornerRadius x:Key="ControlCornerRadius">22</CornerRadius>
        <CornerRadius x:Key="ChipCornerRadius">999</CornerRadius>
        <CornerRadius x:Key="PillCornerRadius">18</CornerRadius>

        <Style x:Key="HeroEyebrowStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{ThemeResource AppAccentSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="HeroTitleStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
            <Setter Property="FontSize" Value="46" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="PageTitleStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
            <Setter Property="FontSize" Value="34" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="CardTitleStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="BodyTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="{ThemeResource AppMutedTextBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="CaptionTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{ThemeResource AppSubtleTextBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>

        <Style x:Key="SurfaceCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource AppSurfaceGradientBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{StaticResource SurfaceCardCornerRadius}" />
            <Setter Property="Padding" Value="24" />
        </Style>

        <Style x:Key="ElevatedSurfaceCardStyle" TargetType="Border" BasedOn="{StaticResource SurfaceCardStyle}">
            <Setter Property="Background" Value="{ThemeResource AppSurfaceStrongGradientBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineStrongBrush}" />
        </Style>

        <Style x:Key="AccentSurfaceCardStyle" TargetType="Border" BasedOn="{StaticResource SurfaceCardStyle}">
            <Setter Property="Background" Value="{ThemeResource AppAccentSoftGradientBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineStrongBrush}" />
        </Style>

        <Style x:Key="SecondarySurfaceCardStyle" TargetType="Border" BasedOn="{StaticResource SurfaceCardStyle}">
            <Setter Property="Background" Value="{ThemeResource AppSecondaryGradientBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineStrongBrush}" />
        </Style>

        <Style x:Key="GlassPanelStyle" TargetType="Border" BasedOn="{StaticResource SurfaceCardStyle}">
            <Setter Property="Background" Value="{ThemeResource AppSurfaceStrongGradientBrush}" />
        </Style>

        <Style x:Key="MiniPillStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource AppSecondaryContainerBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{StaticResource PillCornerRadius}" />
            <Setter Property="Padding" Value="12,8" />
        </Style>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="MinHeight" Value="54" />
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="Padding" Value="20,14" />
            <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="ButtonShell"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}"
                                    Opacity="1"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform x:Name="ButtonScaleTransform" ScaleX="1" ScaleY="1" />
                                </Border.RenderTransform>

                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleX" To="1.015" Duration="0:0:0.18" />
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleY" To="1.015" Duration="0:0:0.18" />
                                            <DoubleAnimation Storyboard.TargetName="ButtonShell" Storyboard.TargetProperty="Opacity" To="0.95" Duration="0:0:0.18" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleX" To="0.975" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleY" To="0.975" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetName="ButtonShell" Storyboard.TargetProperty="Opacity" To="0.88" Duration="0:0:0.12" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShell" Storyboard.TargetProperty="Opacity" To="0.42" Duration="0:0:0.12" />
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeuralVButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{ThemeResource AppAccentGradientBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AppOnAccentBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppAccentSecondaryBrush}" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{ThemeResource AppSecondaryGradientBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineBrush}" />
        </Style>

        <Style x:Key="TertiaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{ThemeResource AppSurfaceGradientBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineBrush}" />
        </Style>

        <Style x:Key="NavTabButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="MinWidth" Value="132" />
            <Setter Property="MinHeight" Value="46" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Background" Value="{ThemeResource AppSurfaceHighBrush}" />
        </Style>

        <Style x:Key="SelectedNavTabButtonStyle" TargetType="Button" BasedOn="{StaticResource NavTabButtonStyle}">
            <Setter Property="Background" Value="{ThemeResource AppPrimaryContainerBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AppOnAccentBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppPrimaryContainerBrush}" />
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Padding" Value="16,14" />
            <Setter Property="MinHeight" Value="58" />
            <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}" />
            <Setter Property="Background" Value="{ThemeResource AppFieldGradientBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="FieldPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Padding" Value="16,14" />
            <Setter Property="MinHeight" Value="58" />
            <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}" />
            <Setter Property="Background" Value="{ThemeResource AppFieldGradientBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppOutlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="FlatListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Application.Resources>
</Application>
